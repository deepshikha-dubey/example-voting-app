pipeline{
    agent {
        label 'jenkins'
    }
    stages{
        stage("Build and Push"){
            steps{
                sh "cd vote"
            }
        }
        stage("Deploy"){
            steps{
                sh "echo kubectl"
            }
        }
    }
}
